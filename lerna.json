{
  "npmClient": "yarn",
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["main"],
      "ignoreChanges": ["**/*.md", "**/test/**", "**/tests/**"],
      "conventionalCommits": true
    },
    "publish": {
      "conventionalCommits": true,
      "registry": "https://npm.pkg.github.com"
    }
  },
  "packages": ["."]
}
